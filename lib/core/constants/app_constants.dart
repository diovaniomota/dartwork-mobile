class AppConstants {
  static const String appName = 'Work ERP';
  static const String appVersion = '1.0.0';
  static const String webAppUrl = 'https://work.dartsistemas.com';
  static const Duration authTimeout = Duration(seconds: 10);
  static const Duration queryTimeout = Duration(seconds: 12);
  static const int defaultPageSize = 20;
}
